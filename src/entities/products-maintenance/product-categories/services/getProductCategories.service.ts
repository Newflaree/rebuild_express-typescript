// Models

const getProductCategoriesService = async ( id: string ) => {
  try {

  } catch ( err ) {
    console.log( `${ '[SERVICE.GET-PRODUCT-CATEGORIES]'.bgRed }: ${ err }` );
  }
}

export default getProductCategoriesService;
