// Models

const updateProductCategoryByIdService = async ( id: string ) => {
  try {

  } catch ( err ) {
    console.log( `${ '[SERVICE.UPDATE-PRODUCT-CATEGORY-BY-ID]'.bgRed }: ${ err }` );
  }
}

export default updateProductCategoryByIdService;
