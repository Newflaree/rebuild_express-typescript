// Models

const createProductCategoryService = async ( id: string ) => {
  try {

  } catch ( err ) {
    console.log( `${ '[SERVICE.CREATE-PRODUCT-CATEGORY]'.bgRed }: ${ err }` );
  }
}

export default createProductCategoryService;
